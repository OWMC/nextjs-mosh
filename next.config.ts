import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // basePath: '/app-router-demo',
  // output: 'export',
  // distDir: 'dist/app-router-demo',
  // images: {
  //   unoptimized: true,
  // },

};

export default nextConfig;
